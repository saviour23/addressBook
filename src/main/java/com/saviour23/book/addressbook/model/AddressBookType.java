package com.saviour23.book.addressbook.model;

/**
 * Address Book Type.
 *
 * Assumption- as of now only Customer type is defined but can be extended.
 */
public enum AddressBookType {
    CUSTOMER
}
